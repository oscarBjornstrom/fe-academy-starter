import { HighlightEvenDirective } from './highlight-even.directive';

describe('HighlightEvenDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightEvenDirective();
    expect(directive).toBeTruthy();
  });
});
